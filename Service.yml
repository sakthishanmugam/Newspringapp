apiVersion: v1
kind: Service
metadata:
  name: newspringapp
spec:
  ports:
  - name: http
    nodePort: 32337
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: LoadBalancer
  selector:
    app: newspringapp
   type: NodePort
