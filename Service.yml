apiVersion: v1
kind: Service
metadata:
  name: newspringapp-deploy
spec:
  ports:
  - name: http
    port: 8081
    protocol: TCP
    targetPort: 8081
  type: LoadBalancer
  selector:
    app: newspringapp-deploy
