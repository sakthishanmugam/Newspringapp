apiVersion: v1
kind: Service
metadata:
  name: my-application
spec:
  ports:
  - name: http
    port: 8090
    protocol: TCP
    targetPort: 8090
  type: LoadBalancer
  selector:
    app: my-application
   
