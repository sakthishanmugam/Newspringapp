apiVersion: v1
kind: Service
metadata:
  name: newspringapp-deployment
spec:
  ports:
  - name: http
    port: 9090
    protocol: TCP
    targetPort: 9090
  type: LoadBalancer
  selector:
    app: newspringapp-deployment
